require "spec_helper"

describe UsersController do
    # befor do
    #     @user = build(:user)
    #     post "/login", {name: @user.name, password: @user.password}
    # end
    
    describe "GET #index" do
        it "has logging users infomation"
        it "shows @user's weights records graph"
        it "shows @user's BMI"
        it "shows @user's last weight"
    end
    
end